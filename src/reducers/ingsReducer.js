const initialState = {
  ings: null
};

const ingsReducer = (state = initialState) => {
  switch (action.type) {
    case "":
      return {};
    default:
      return state;
  }
};

export default ingsReducer;
